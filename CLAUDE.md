@AGENT.md

The repo context lives in `AGENT.md` and nowhere else — the line above imports
it, so there is one file to maintain rather than two. Put anything you would have
written here in `AGENT.md` instead.
